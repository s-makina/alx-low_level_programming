#include "main.h"
#include "<string.h>"

/**
 * _strlen - returns the length of a string
 * @s: Char pointer
 * Return: Int, String size
 */

int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
