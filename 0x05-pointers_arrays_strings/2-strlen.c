#include "main.h"
#include <stdio.h>

/**
 * _strlen - returns the length of a string
 * @s: Char pointer
 * Return: Int, String size
 */

int _strlen(char *s)
{
	int len = 0;

	while (s[len])
	{
		len += 1
	}
	return (len);
}
